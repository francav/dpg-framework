{
  "name": "dpg-framework",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "echo \"no site generator yet (SSG deferred)\"",
    "lint": "npm run format:check",
    "test": "echo \"no tests yet\"",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3"
  }
}
